Fix the measurement annotation lines on the Features & Size tab product images.
Do NOT touch any backend files.

Look at the current implementation in the Features & Size tab component
(likely in app/(store)/products/[slug]/page.tsx or a separate component).

=================================================================
PROBLEM 1 — FRONT VIEW: RIGHT SIDE VERTICAL LINE MISSING NUMBER
=================================================================

CURRENT: On the Front view image, the right side shows a vertical 
dashed line (for lens height) but the number (mm value) is NOT 
displaying next to it — it shows blank or cut off.

FIX:
- The vertical line on the RIGHT side of the image shows lens_height_mm
- Make sure the label "55mm" (or whatever lens_height_mm value is) 
  appears clearly next to the vertical line
- Label position: to the RIGHT of the vertical line, vertically centered
- If the label is being cut off by the image container: 
  * Add overflow: visible to the image wrapper
  * OR add right padding to the container so label has space
  * OR position label INSIDE the image (absolute positioned) 
    at the right edge, with white/orange background pill

The label must show: "[lens_height_mm]mm" in orange color
Same style as the other labels (45mm, 30mm shown in orange at top)

=================================================================
PROBLEM 2 — SIDE VIEW: NO MEASUREMENT LINE AT ALL
=================================================================

CURRENT: The Side view image shows the product photo but has 
NO measurement lines or annotations on it.

FIX:
- On the Side view image, add ONE measurement line:
  * A horizontal dashed line showing the TEMPLE LENGTH
  * The line runs horizontally across the temple arm of the glasses
  * Label: "[temple_mm]mm" (e.g. "150mm") 
  * Position: somewhere across the middle-upper area of the image
    where the temple arm is visible
  * Line color: orange dashed (#E8670A)
  * Label style: orange color, small text, same as front view labels

- Since the side image position varies per product photo, place the 
  temple line at approximately:
  * Top: 40% of image height (this is where temple arm usually appears)
  * Left: 10% to Right: 70% of image width (spanning across temple)
  * Label at left end of line: "[temple_mm]mm"

=================================================================
PROBLEM 3 — BACKGROUND/GREY IMAGES: LINES NOT SHOWING
=================================================================

CURRENT: When the product image has a grey or dark background,
the measurement lines and labels are invisible or hard to see.

FIX:
- The annotation lines and labels must be visible on ANY background
- Change the line and label style to always be visible:

  For LINES:
  - Keep orange dashed color: stroke="#E8670A" stroke-dasharray="6,4"
  - Add a subtle white/light stroke underneath for contrast on dark bg:
    Use a white line behind orange line (same path, white, slightly thicker)
  - OR: use orange color which is visible on both light and dark backgrounds

  For LABELS (the mm numbers):
  - Wrap each label in a small pill/badge:
    * Background: white (#ffffff) 
    * Text color: orange (#E8670A) or dark (#1a1a1a)
    * Border: 1px solid #E8670A
    * Border-radius: 4px
    * Padding: 1px 5px
    * Font-size: 11px, font-weight: 600
  - This pill/badge makes labels readable on ANY background color
    (grey, white, black, beige — all work with white pill + orange text)

  For ARROW ENDPOINTS on lines:
  - Make arrowheads orange colored
  - They should be clearly visible

SUMMARY of label style change:
Instead of plain orange text floating over image,
use: white background pill with orange border and orange text.
This ensures visibility on grey/dark product images.

=================================================================
ALSO FIX — SIZE SHOWS "Null" IN FEATURES TAB
=================================================================

CURRENT: Size shows "Null□14-144" (Null instead of size label)

FIX:
- The size display uses: "[size_label]□[lens_width]-[temple]"
- size_label is coming as null for some products
- Fix: if size_label is null or empty, show just the measurements:
  "[lens_width]□[bridge]-[temple]" without the null prefix
- Example: instead of "Null□14-144" show "□14-144" 
  OR better: show "One Size (□14-144)" as fallback
- In the spec list on left side, Size row:
  if variant.size_label exists: "[size_label] (□[lens_width][bridge]-[temple])"
  if variant.size_label is null/empty: "□[lens_width]-[bridge]-[temple]"

=================================================================
IMPLEMENTATION NOTES
=================================================================

The annotation overlay is likely implemented as an SVG or absolutely 
positioned divs over the image container.

Find the component that renders the Front/Side image with overlays.
Make targeted fixes only to:
1. The right-side vertical line label (Problem 1)
2. Add temple line to Side view (Problem 2)  
3. Label pill styling for visibility on any background (Problem 3)
4. Null size_label fix (bonus fix)

After fixing:
git add .
git commit -m "fix: measurement annotations - side line, label visibility, null size"
git push