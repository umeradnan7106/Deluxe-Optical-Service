Do NOT change backend files unless specifically mentioned below.
Implement ALL changes completely — do not skip or partially implement anything.

=================================================================
CHANGE 1 — PRODUCT PAGE: FEATURES & SIZE TAB — IMAGE ANNOTATIONS
=================================================================

In the Features & Size tab, the right column shows product images.

CURRENT: Images show without any annotations.

REQUIRED:

A) FRONT VIEW IMAGE (First image — images[0]):
   - Show the product's first image in a white bordered box
   - Overlay dashed measurement lines ON TOP of the image using 
     absolutely positioned SVG or HTML elements
   - The dashed lines must show these 3 measurements:
     * TOP horizontal dashed line: shows "lens_width mm" label 
       (e.g. "45mm") — positioned across the lens area at top
     * MIDDLE horizontal dashed line: shows "bridge mm" label 
       (e.g. "30mm") — positioned at bridge area
     * LEFT vertical dashed line: shows "lens height mm" or frame height
   - The actual mm numbers come from the product data:
     product.lens_width, product.bridge, product.temple
   - Lines are STATIC SVG overlay — always in same position 
     (since all frame images will be consistent size)
   - Style of lines: dashed (#333), with small arrow ends, 
     label text in small dark pill (like the reference screenshot)
   - Below this image: "Front" tab (active by default)

B) SIDE VIEW IMAGE (Second image — images[1]):
   - Do NOT show the side image below the front image
   - Instead: show "Front" and "Side" as two clickable tabs/buttons
     below the image box
   - "Front" tab active = shows images[0] with measurement overlays
   - "Side" tab active = shows images[1] WITHOUT measurement overlays
     (just clean side view image)
   - If only 1 image exists: hide the "Side" tab
   - Tab style: small text buttons, active = orange underline

C) MEASUREMENTS CAPTION:
   - Below the image box, show:
     "[lens_width]mm lens · [bridge]mm bridge · [temple]mm temple"
   - Small muted text, centered

=================================================================
CHANGE 2 — ADMIN ORDERS: SHOW LENS & PRESCRIPTION DETAILS
=================================================================

CURRENT: Admin order detail does not show lens customization or 
prescription information.

REQUIRED:

In the Admin Orders detail page (/admin/orders/[id]):

A) Add a "LENS & PRESCRIPTION" section/card between the frame details 
   and payment strip. Show these fields:

   LENS DETAILS:
   - Lens Type: [order.lens_type] (e.g. "Single Vision")
   - Lens Sub-type: [order.lens_sub_type] (only if progressives)
   - Coating: [order.coating] (e.g. "Anti-Reflective")  
   - Add-ons: [order.add_ons joined by comma] (e.g. "Scratch Resistant, UV400")
   - Lens Price: Rs. [order.lens_price]
   - Coating Price: Rs. [order.coating_price]

   PRESCRIPTION:
   - Method: [order.prescription_method] 
     (show as badge: "Manual Entry" / "Photo Upload" / "None")
   - If method = "manual": show a clean table:
     |      | SPH   | CYL   | Axis |
     | OD   | +1.25 | -0.50 | 90   |
     | OS   | +1.00 | -0.25 | 85   |
     ADD: +1.00  |  PD: 64mm
   - If method = "upload": show the prescription image as a thumbnail
     with a "View Full Size" link that opens image in new tab
   - If method = "none": show "No prescription required"

B) In the Admin Orders LIST page (/admin/orders):
   - Add a small lens indicator badge on each order row
   - If order has lens customization: show small orange "Lens" badge
   - If order has prescription: show small blue "Rx" badge
   - These badges help admin quickly identify which orders need 
     lens preparation

C) Do NOT add any shipping/courier system — leave shipping as is.

=================================================================
CHANGE 3 — ADMIN: DRAFT ORDER CREATION
=================================================================

Add ability for admin to manually create orders (for WhatsApp/phone orders).

A) In Admin Orders list page (/admin/orders):
   - Add "Create Draft Order" button next to existing buttons at top right
   - Button style: orange, with "+" icon

B) Create new page: /admin/orders/new (Draft Order Creation):

   Layout: same as regular order detail page

   SECTION 1 — Customer Information:
   - Search existing customers by name/email/phone (typeahead search)
   - OR fill in manually:
     * Full Name (required)
     * Phone (required)
     * Email (optional)
     * Address, City, Province (required)
     * Order Notes (optional)

   SECTION 2 — Product Selection:
   - Search products by name or SKU (typeahead search)
   - Select product → shows color/size variant dropdown
   - Shows product image + name + selected variant
   - Quantity input

   SECTION 3 — Lens Customization (optional):
   - Toggle: "Include Lens Customization?" (Yes/No)
   - If Yes: show same lens selection as customer-facing select-lenses page
     * Lens Type dropdown (from lens options)
     * Coating dropdown
     * Prescription Method radio (Manual / Upload / None)
     * If Manual: show prescription entry form (SPH/CYL/Axis dropdowns)

   SECTION 4 — Payment:
   - Payment Method dropdown (COD, EasyPaisa, JazzCash, Bank Transfer)
   - Payment Status: Pending / Paid
   - Apply Coupon (optional)
   - Shows calculated total (frame + lens + discount + shipping)

   SECTION 5 — Order Summary:
   - Shows complete summary before saving
   - "Create Order" button (orange, full width)
   - On save: creates order with status "placed", shows success message,
     redirects to the new order's detail page

   This page calls POST /api/admin/orders/draft to create the order.
   Backend: add POST /api/admin/orders/draft endpoint that accepts
   all order fields and creates an order with admin-assigned data.

=================================================================
CHANGE 4 — ADMIN: ABANDONED CARTS PAGE
=================================================================

Add a new "Abandoned Carts" section in the admin panel.

A) Add "Abandoned Carts" link to admin sidebar under Orders section.
   Show count badge of active abandoned carts.

B) Create new page: /admin/abandoned-carts

   PAGE HEADER:
   - Title: "Abandoned Carts"
   - 4 stat cards at top:
     * Active Abandoned (count)
     * Recovered (count — orders that were eventually placed)
     * At-Risk Revenue (total value of abandoned carts)
     * Recovered Revenue (total value of recovered orders)

   FILTER TABS: "All" | "Active" | "Recovered"

   CART LIST (each abandoned cart as a card):
   - Header row: 
     * Status badge (Active/Recovered)
     * Customer email or "Guest" if no email
     * Number of items + total value (e.g. "2 items — Rs. 2,500")
     * Date abandoned (e.g. "Abandoned May 29, 2026, 7:00 PM")
     * "Send Reminder" button (orange) — sends WhatsApp message or email
     * "Hide" button (grey) — hides from active list
   
   - Expandable section (click "View items" to expand):
     Shows table:
     | Product | SKU | Color/Size | Qty | Price | Total |
     
     Shows cart total at bottom right
   
   - If lens was selected: show lens details in expanded view too

   C) How abandoned carts are tracked:
   - When a customer adds to cart and doesn't checkout within 2 hours,
     it appears here
   - Cart data is saved to abandoned_carts table in database
   - Backend: the abandoned_carts table already exists from initial migration
   - Backend: add GET /api/admin/abandoned-carts endpoint that returns
     all abandoned carts with cart_data parsed
   - Frontend: parse cart_data JSON to show product details

   D) "Send Reminder" button:
   - For now: opens WhatsApp with pre-filled message:
     "Hi! You left items in your cart at DeluxeOpt. 
      Complete your order here: [link to cart]"
   - Opens WhatsApp web link in new tab
   - Uses customer phone number if available

=================================================================
CHANGE 5 — WISHLIST FIX
=================================================================

CURRENT: Clicking heart icon does not add products to wishlist.

REQUIRED: Fix wishlist functionality completely.

A) In ProductCard component:
   - Heart icon: outline by default, filled orange when in wishlist
   - On click: 
     * If user NOT logged in: redirect to /login?redirect=[current page]
     * If user IS logged in: 
       - If NOT in wishlist: call POST /api/wishlist/{product_id}
         → add to wishlist → heart becomes filled orange
       - If already in wishlist: call DELETE /api/wishlist/{product_id}
         → remove from wishlist → heart becomes outline
   - Show brief success toast: "Added to wishlist" / "Removed from wishlist"

B) In Product Detail page:
   - Same heart icon behavior (larger icon near product title)
   - Same add/remove logic

C) On page load:
   - If user is logged in, fetch GET /api/wishlist to get wishlist product IDs
   - Store wishlist IDs in Zustand wishlistStore
   - All heart icons check against this store to show filled/outline

D) Wishlist store (create frontend/store/wishlistStore.ts):
   - State: productIds (array of product IDs in wishlist)
   - Actions: addToWishlist, removeFromWishlist, setWishlist, isInWishlist
   - Persist wishlist IDs in localStorage for logged-in users

E) In /account/wishlist page:
   - Fetch GET /api/wishlist and show products as product cards
   - Each card has heart icon (filled) that removes from wishlist on click
   - If empty: show "Your wishlist is empty" message with browse button

F) Wishlist count badge:
   - In Header, next to wishlist icon, show count badge if items > 0
   - Same style as cart badge (orange circle)

=================================================================
CHANGE 6 — PROFILE & SETTINGS PAGE REDESIGN
=================================================================

CURRENT: Profile page looks basic and unattractive with black background cards.

REQUIRED: Redesign /account/profile (or /account/settings) to look professional.

Layout: same sidebar nav as other account pages

Page content (white background, clean cards):

CARD 1 — "Personal Information":
- Background: white, border #e5e7eb, rounded-lg, p-6
- Top: user avatar circle (large, 64px, orange bg with initials)
  next to: user's full name (large) + email (muted) + "Member since [date]"
- Form below:
  * Full Name (input, pre-filled)
  * Email (input, read-only, show lock icon)
  * Phone (input, pre-filled)
  * "Save Changes" button (orange)

CARD 2 — "Change Password":
- Background: white, border, rounded-lg, p-6
- Current Password input
- New Password input  
- Confirm New Password input
- Password strength indicator (weak/medium/strong)
- "Update Password" button (orange)

CARD 3 — "Account Activity" (just info display, no form):
- Background: white, border, rounded-lg, p-6
- Show: Total Orders | Total Spent | Member Since | Last Login
- 4 small stat boxes in a row

CARD 4 — "Danger Zone":
- Background: white, border border-red-200, rounded-lg, p-6
- "Sign Out" button (red outline)
- Simple, clean layout

Remove the black background from ALL cards on this page.
All card backgrounds: white (#ffffff)
All text: dark (#1a1a1a)
All input fields: white background, dark text

=================================================================
CHANGE 7 — ADMIN CUSTOMERS: SHOW REGISTERED CUSTOMERS
=================================================================

CURRENT: Customers page shows no data even though users exist.

REQUIRED: Fix admin customers page to show actual data.

A) Fix the API call:
   - The customers page calls GET /api/admin/customers
   - Backend: check if this endpoint exists in backend/routes/admin/
   - If it doesn't exist: create it
   - Endpoint should return all users from the users table
   - For each user, also count their orders and sum their order totals
   - Response format per customer:
     {
       id, full_name, email, phone, created_at,
       total_orders: count of orders by this user,
       total_spent: sum of order totals by this user,
       last_order_date: most recent order date
     }

B) Show TWO tabs on customers page:
   
   Tab 1 — "Registered Customers":
   - Shows users from users table (is_admin = false)
   - Table columns: Name | Email | Phone | Orders | Total Spent | Joined | Actions
   - Click row → see customer's order history
   - Sort by: Total Spent (default, descending)
   
   Tab 2 — "Guest Orders":
   - Shows orders where user_id is NULL
   - Groups by customer_phone (same phone = same guest customer)
   - Shows: Name | Phone | Email | Order Count | Total Spent | Last Order
   - Sort by: Total Spent (default, descending)

C) Customer Detail View (click any customer):
   - Shows customer info at top
   - Shows all their orders in a table below
   - Each order: Order ID | Date | Items | Total | Status

=================================================================
CHANGE 8 — ADMIN INVENTORY: STOCK EDIT FUNCTIONALITY
=================================================================

CURRENT: Inventory page shows stock levels but cannot edit them.

REQUIRED: Add inline stock editing.

In /admin/inventory page:

A) Each row in the inventory table:
   - Stock column: shows current stock number
   - On CLICK of the stock number → transforms into an editable input field
   - User types new stock number
   - Press Enter OR click "Save" checkmark button → saves to backend
   - Press Escape OR click "×" → cancels edit, reverts to original
   - On save: calls PUT /api/admin/inventory/{variant_id} with {stock: newValue}
   - On success: update the displayed stock number, show brief green success indicator
   - On error: show red error message

B) Also add a "Quick Update" button per row:
   - Small edit (pencil) icon button at end of each row
   - Click → opens a small inline edit mode for that row
   - Same behavior as clicking stock number

C) Bulk stock update:
   - Checkbox on each row (select multiple variants)
   - When checkboxes selected: show "Update Selected Stock" button at top
   - Click → opens modal: "Set stock to: [input]" for all selected variants
   - Useful when restocking multiple items at once

=================================================================
CHANGE 9 — ADMIN PRODUCTS: IMAGES GROUPED BY COLOR
=================================================================

CURRENT: Product images show as a flat list with only first tagged "Main".

REQUIRED: Images should be organized by color variant (like reference screenshot).

In Add/Edit Product form, the "PRODUCT IMAGES" section:

A) After admin has added colors in the Variants section,
   the images section groups by color:

   Display:
   ● Black   3 images
   [▲][▼] [thumbnail] Set Primary | Color: [Black dropdown] | [🗑 delete]
   [▲][▼] [thumbnail] Set Primary | Color: [Black dropdown] | [🗑 delete]
   [▲][▼] [thumbnail] Set Primary | Color: [Black dropdown] | [🗑 delete]
   
   ● Gold    2 images
   [▲][▼] [thumbnail] PRIMARY     | Color: [Gold dropdown]  | [🗑 delete]
   [▲][▼] [thumbnail] Set Primary | Color: [Gold dropdown]  | [🗑 delete]

   "+ Add Media" button at bottom to upload more images

B) Each image row has:
   - Up/Down arrows (▲▼) to reorder images within that color group
   - Thumbnail preview (64x56px, object-contain)
   - "PRIMARY" badge if it's the main image for that color
     OR "Set Primary" button to make it primary
   - Color dropdown: shows all colors added in variants section
     (admin can assign/reassign image to any color)
   - Delete button (red trash icon)

C) The "PRIMARY" image for each color is what shows on product page
   when customer selects that color.

D) When no variants/colors added yet:
   - Show simple flat image list without color grouping
   - Images labeled: "1st (Main)", "2nd (Gallery)", "3rd (Description)"

E) Image upload:
   - "Add Media" button or drag-drop zone at bottom
   - Upload goes to Cloudinary via POST /api/upload/image
   - Newly uploaded images go to "Unassigned" group initially
   - Admin then selects which color they belong to

F) Backend changes needed:
   - ProductImage model needs a color_name field (nullable) 
     to store which color variant this image belongs to
   - Add color_name to ProductImage schema
   - Create Alembic migration for this field
   - Update PUT /api/admin/products/{id}/images/reorder to handle 
     color assignments

=================================================================
CHANGE 10 — ADMIN PRODUCTS: VARIANTS FIX
=================================================================

CURRENT: Variants section is not saving or showing data correctly.

REQUIRED: Fix variants completely so they work properly.

In Add/Edit Product form, the "VARIANTS (COLORS & SIZES)" section:

A) DISPLAY: Show existing variants correctly
   - When editing a product, pre-populate the variants table with 
     all existing variants from the API
   - Each row shows all fields: Color Name, Color hex, Size Label,
     Lens Width, Bridge, Temple, SKU, Price, Stock, Active

B) ADD NEW VARIANT:
   - "+ Add Color / Size" button adds a new empty row
   - User fills in all fields
   - Row is saved when "Save Changes" / "Save Product" is clicked

C) EDIT EXISTING VARIANT:
   - All fields in existing rows are editable inline
   - Changes are saved when "Save Changes" button is clicked

D) DELETE VARIANT:
   - "×" button on each row deletes that variant
   - Confirmation: "Are you sure?" before deleting
   - If only 1 variant: cannot delete (show warning)

E) SAVING: When "Save Changes" or "Save Product" is clicked:
   - ALL variants (new, edited, deleted) are processed
   - New variants: POST /api/admin/products/{id}/variants
   - Updated variants: PUT /api/admin/products/{id}/variants/{vid}
   - Deleted variants: DELETE /api/admin/products/{id}/variants/{vid}
   - Show success message after save
   - Show error message if any variant fails to save

F) COLOR HEX PICKER:
   - The color hex field must have a working color picker input
   - Shows a colored square preview of the selected color
   - Clicking square opens native color picker

G) SIZE LABEL:
   - Dropdown with options: One Size, XS, S, M, L, XL, XXL, 
     Small, Medium, Large, Extra Large
   - OR free text input if admin wants custom label

H) AUTO-GENERATE SKU:
   - When Color Name and Size Label are filled:
   - Auto-suggest SKU: [product.sku]-[first 3 letters of size]-[first 3 letters of color]
   - Example: DOS-SG-001-SML-BLK
   - Admin can override this

=================================================================
CHANGE 11 — LENS OPTIONS: PROGRESSIVES SUB-OPTIONS + REMOVE ADD-ONS FROM MODAL
=================================================================

A) REMOVE Add-ons type from Lens Options modal:
   - In New/Edit Lens Option modal, remove "Add-ons" from the Type dropdown
   - Type dropdown should only have: "Lens Type" and "Coating"
   - The Add-ons tab and its content: COMPLETELY REMOVE from admin Lens Options page
   - Remove the "Add-ons" tab from the Lens Options page tabs
   - Only keep: "Lens Types" tab and "Coatings" tab

B) PROGRESSIVES SUB-OPTIONS in New/Edit Lens Option modal:
   - When Type = "Lens Type" AND Name contains "Progressive" (or admin checks a checkbox):
     show a "Sub-options" section in the modal
   - OR: add a checkbox "Has sub-options (e.g. Progressives)" that when checked 
     reveals the sub-options section

   Sub-options section shows:
   - Label: "Sub-options (tiers)"
   - A table/list where admin can add multiple sub-option rows
   - Each row has:
     * Name input (e.g. "Premium Progressive")
     * Price input (Rs.)
     * Description input (short, optional)
     * "Recommended" checkbox (shows recommended badge to customer)
     * Delete row button (×)
   - "+ Add Sub-option" button to add more rows
   - These sub-options save as JSON in a sub_options field on the LensOption

   Backend changes:
   - Add sub_options JSON field to LensOption model
   - Create Alembic migration for this field
   - Update LensOption schema to include sub_options
   - Update PUT/POST /api/admin/lens-options to save sub_options

C) LENS OPTIONS MODAL — Make it larger and better:
   - Modal width: 600px (wider than current)
   - All fields properly spaced with labels
   - Fields in modal:
     * Name (text input, full width)
     * Type (dropdown: Lens Type | Coating)
     * Price (Rs.) (number input)
     * Description (textarea, 3 rows)
     * Sort Order (number input)
     * Has Sub-options (checkbox — only shows when Type = Lens Type)
     * Sub-options table (shows when checkbox checked)
     * Active (checkbox)
   - Save and Cancel buttons at bottom
   - Modal should be scrollable if content is long

=================================================================
IMPLEMENTATION NOTES
=================================================================

- Start with Change 5 (Wishlist) and Change 10 (Variants) as they are 
  most user-facing and critical
- Change 3 (Draft Orders) and Change 4 (Abandoned Carts) require 
  new backend endpoints — create them
- Change 9 (Image by color) requires backend migration — do it
- Change 11 (Sub-options) requires backend migration — do it
- Do NOT break any existing working functionality
- After all changes: git add . && git commit -m "feat: admin improvements, wishlist fix, abandoned carts, draft orders" && git push
- Test each change after implementing before moving to next